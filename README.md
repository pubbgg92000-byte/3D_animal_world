# Wild Trails

An interactive React Three Fiber nature experience. A seeded world generator builds a new meadow on each refresh from reusable forest and wildlife GLB assets, with an animated moose as the main character.

Add `?seed=your-seed` to the URL to reproduce a particular world.

```bash
npm run dev
npm run build
npm run lint
```
